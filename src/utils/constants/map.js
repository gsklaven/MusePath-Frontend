/**
 * Map Configuration Constants
 * 
 * Default settings for interactive museum map display.
 * 
 * @module utils/constants/map
 */

/**
 * Default map configuration values.
 * 
 * @constant {Object}
 * @property {number} ID - Default map identifier (ground floor)
 * @property {number} ZOOM - Initial zoom level (1 = default view)
 * @property {number} ROTATION - Initial rotation angle in degrees
 */
export const MAP_DEFAULTS = {
  ID: 1,
  ZOOM: 1,
  ROTATION: 0,
};
